name: Truck Junction Automation Test Workflow

on:
  pull_request:
    branches: [main, master, develop]
  schedule:
    - cron: '30 21 * * *' # 21:30 UTC = 03:00 AM IST
  workflow_dispatch: {}

env:
  NODE_VERSION: '24'

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Cache dependencies and Cypress binary
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-node${{ env.NODE_VERSION }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci

  cypress-run:
    runs-on: ubuntu-latest
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        chunk:
          - ui-homepage
          - ui-pdp
          - ui-listing
          - ui-category-a
          - ui-category-b
          - ui-used-buses
          - ui-utility
          - ui-lead-suite
          - ui-login
          - seo-home-listing
          - seo-category-a
          - seo-category-b
          - seo-pdp-buses
          - seo-used
          - seo-utility
        device: [desktop, mobile]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Restore cached dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-node${{ env.NODE_VERSION }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies (cache miss fallback)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Run Cypress chunk (${{ matrix.chunk }} — ${{ matrix.device }})
        env:
          DEVICE: ${{ matrix.device }}
        run: node scripts/run-ci-chunk.js ${{ matrix.chunk }}

      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: screenshots-${{ matrix.chunk }}-${{ matrix.device }}
          path: cypress/screenshots
          if-no-files-found: ignore

      - name: Upload videos
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: videos-${{ matrix.chunk }}-${{ matrix.device }}
          path: cypress/videos
          if-no-files-found: ignore

      - name: Upload Allure results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-${{ matrix.chunk }}-${{ matrix.device }}
          path: allure-results
          if-no-files-found: ignore

      - name: Upload SEO structure Excel report
        uses: actions/upload-artifact@v4
        if: always() && startsWith(matrix.chunk, 'seo-')
        with:
          name: seo-structure-report-${{ matrix.chunk }}-${{ matrix.device }}
          path: artifacts/seo-structure-report.xlsx
          if-no-files-found: ignore

  # Nightly (03:00 IST) and manual workflow_dispatch only — not on PRs.
  # Log-only sitemap URL health: cy.request on each <loc> inside the 8 XMLs.
  # Runs in parallel with ui/seo. continue-on-error so a timeout here does
  # not block the combined Allure report from ui/seo.
  sitemap-run:
    runs-on: ubuntu-latest
    needs: setup
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    timeout-minutes: 360
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Restore cached dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-node${{ env.NODE_VERSION }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies (cache miss fallback)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Run sitemap HTTPS URL health (log only)
        run: npm run test:sitemap

      - name: Upload sitemap Allure results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-sitemap
          path: allure-results
          if-no-files-found: ignore

  publish-report:
    runs-on: ubuntu-latest
    needs: [setup, cypress-run, sitemap-run]
    if: >-
      always() && !cancelled() && needs.setup.result == 'success' &&
      (needs.sitemap-run.result == 'success' || needs.sitemap-run.result == 'skipped' || needs.sitemap-run.result == 'failure')
    env:
      REPORTS_DEPLOY_TOKEN: ${{ secrets.REPORTS_DEPLOY_TOKEN }}
      REPORT_EMAIL_API_KEY: ${{ secrets.REPORT_EMAIL_API_KEY }}
      REPORT_EMAIL_RECIPIENTS: ${{ vars.REPORT_EMAIL_RECIPIENTS }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Restore cached dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-node${{ env.NODE_VERSION }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies (cache miss fallback)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Download Allure results (all chunks)
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: allure-results-*
          path: allure-results
          merge-multiple: true

      - name: Download sitemap Allure results
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: allure-results-sitemap
          path: allure-results

      - name: Download failure screenshots (all chunks)
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: screenshots-*
          path: cypress/screenshots
          merge-multiple: true

      - name: Download SEO structure Excel reports (all chunks)
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: seo-structure-report-*
          path: artifacts/seo-structure-per-chunk
          merge-multiple: false

      - name: Merge SEO structure Excel reports into one file
        run: |
          shopt -s nullglob
          sources=(artifacts/seo-structure-per-chunk/*/seo-structure-report.xlsx)
          if [ ${#sources[@]} -eq 0 ]; then
            echo "No per-chunk SEO structure Excel reports found — skipping merge."
          else
            node scripts/merge-seo-structure-excel.js artifacts/seo-structure-report.xlsx "${sources[@]}"
          fi

      - name: Upload merged SEO structure Excel report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: seo-structure-report
          path: artifacts/seo-structure-report.xlsx
          if-no-files-found: ignore

      - name: Generate Allure HTML report
        if: always()
        run: npm run generate:report

      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: html-report
          path: allure-report

      - name: Publish report to GitHub Pages history
        if: ${{ always() && env.REPORTS_DEPLOY_TOKEN != '' }}
        run: npm run report:publish

      - name: Send report email
        if: ${{ always() && env.REPORT_EMAIL_API_KEY != '' }}
        run: npm run report:email
